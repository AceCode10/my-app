
'use client';
// This component now acts as a re-export of the main editor.
// The `withRole` HOC on the original component will handle permissions.
export { default } from '@/app/(dashboard)/teacher/notes/[id]/page';
